To start the application inside docker use:
```
docker-compose up
```

To be in developer mode in Svelte use:
```
cd frontend-svelte
npm run dev
```

To be in developer mode in Express use:
```
cd Backend
npm run start
```